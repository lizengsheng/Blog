# 进度记录

## 2026-09-15：依赖安全修复与发布复核

- 简历发布审计触发共同技术栈检查后，更新现有版本范围内的 Astro/Markdown/样式与构建依赖，Astro 升至 7.3.2，sharp 升至 0.35.4，不跨主要版本升级。
- 对上游固定的间接依赖补充限定版本范围的覆盖：lodash-es 4.18.1、js-yaml 4.3.2、smol-toml 1.7.1；pnpm audit 最终为 0 漏洞，保留现有构建与审计要求。
- 修复后 pnpm run verify 再次通过：59 个文件诊断无错误/警告/提示，36 个静态页面、6 篇搜索文章。
- 修复后完整浏览器回归通过：六篇文章的 119 处公式与 11 张图表、主题切换、跨文章导航、代码复制、无脚本/错误图表回退、搜索与旧路由清理均正常；两项目在 1280/390/320px 无页面横向溢出，浏览器无脚本错误。
- 首轮线上验证六篇文章、119 处公式、11 张 Mermaid 图、AfterPilot 搜索均正常，浏览器无脚本错误；线上链接巡检 47 个站内页面/资源无失效，外部链接无明确失效，部分站点受反爬或网络限制无法自动验证。
- CodeAgent-RL 与 AfterPilot 简历已成功部署并完成三种宽度与详情链接验证，PDF 保留原有未提交修改。

## 2026-09-14：六篇技术长文与公式、图表支持

- 按用户材料发布 AfterPilot、CodeAgent-RL、KG-Deepsearch、MedQwen、LeetCode Hot100、大模型面试手写实现六篇完整长文，删除原三篇专题提纲；CodeAgent-RL 设为精选。
- 项目文章统一标题、摘要、目录与实验口径，区分实测、预期、配置建议和待核实冲突。AfterPilot 对应用户提供的 PROJECT_SUMMARY.md；医疗文章保留数据隔离和评测定义限制。
- 两篇算法长文保留全部题目/实现章节、原图片链接和 162 个 Python 代码块；AST 检查通过，修正示例的 9 个断言及 PPO/GRPO 独立数值对照通过。未运行完整 PyTorch 训练或在线判题。
- 加入 remark-math、rehype-katex、KaTeX 与 Mermaid；119 处公式构建时渲染，11 张图表按需加载，支持主题与页面切换，禁用脚本或渲染失败时保留源码。长公式修正左侧裁切，表格和图表在容器内横向滚动。
- 本地验证：pnpm run verify 通过，Astro 59 个文件诊断为 0 错误/警告/提示；生产生成 36 页，Pagefind 索引 6 篇文章，六组关键词均命中对应文章。
- 浏览器检查：桌面/390px 阅读、目录锚点、明暗主题、页面切换、代码复制、错误图表回退及无 JavaScript 公式/图源均正常，控制台无脚本错误；旧三篇路由返回 404。
- 临时 MDX 文章验证行内/独立公式与 Mermaid 后已删除；修正 MDX 高亮器替换 pre 类名造成的图源识别问题。四篇含公式文章的左侧裁切检查均为 0。
- 本地链接巡检的 46 个站内页面/资源无失效；外部参考链接无明确失效，6 个百科链接受 403 限制。发布前新增页面的线上 canonical 返回 404，部署后需按现有发布流程复核。
- Mermaid 延迟加载包触发 Vite 的体积提示，普通无图表页面不主动加载图表运行库。沿用 main 推送触发 GitHub Pages 发布。

## 2026-08-19：项目基线

- 从 AstroPaper `main` 分支初始化项目，保留 `upstream` 远程用于后续参考。
- 建立项目级 `AGENTS.md` 和完整 memory-bank。

## 2026-08-19：中文化与个人品牌

- 配置 `zh-CN`、`Asia/Shanghai`、`/Blog` 基路径和 GitHub Pages 站点地址。
- 完成导航、搜索、分页、文章元信息、标签、归档、404、分享与无障碍文本的中文化。
- 重写首页与 About，只保留已确认的教育、方向、邮箱、GitHub 和公开学习仓库信息。
- 新增 LZS SVG favicon 与静态 PNG 社交预览图，关闭动态中文 OG 图生成。
- 修正 RSS、robots、canonical、站点地图、搜索资源和搜索结果 URL 的子路径处理。

## 2026-08-19：首发内容与写作流程

- 删除 AstroPaper 演示文章和模板个人信息。
- 发布后训练、Agent 工程、Agentic RL 三篇公开的“持续更新”技术提纲。
- 新增不参与构建的 `_template.md` 和中文 README；引用仅使用论文或官方文档。

## 2026-08-19：部署与验证

- 新增 GitHub Pages 部署工作流；PR CI 使用 Node 24 和 pnpm 11.19.0。
- 将 Unix 专用的 Pagefind 复制命令替换为跨平台 Node 脚本。
- `pnpm install --frozen-lockfile`、格式检查、lint 和完整构建通过；Astro 检查为 0 错误、0 警告、0 提示。
- 生产构建生成 20 个页面；Pagefind 识别 `zh-cn`，索引 3 篇文章和 747 个词。
- 本地预览验证主页、文章、标签、归档、搜索、About、RSS、站点地图和静态资源均返回 200。
- 生成产物检查确认 URL 均位于 `/Blog/`，中文 Pagefind 查询可返回正确文章。
- 完成桌面浅色、桌面深色和 390px 移动端视觉检查；移动端无文档级横向溢出。

## 2026-08-19：在线简历入口

- 在全站顶部导航加入“简历”入口，使用新窗口打开在线简历。
- 在首页首屏加入高可见度的“查看在线简历”按钮，并在 About 联系方式中同步展示。

## 2026-08-19：远程仓库与正式部署

- 创建公开仓库 `https://github.com/lizengsheng/Blog`，将当前博客快照作为独立项目的根提交推送到 `main`。
- 保留 AstroPaper MIT 许可证和 `upstream` 远程，不携带模板仓库的工作流历史。
- 通过仓库网页添加 Pages 部署与 PR CI 工作流，并同步回本地 `main`。
- 将 GitHub Pages Source 设置为 GitHub Actions，正式地址为 `https://lizengsheng.github.io/Blog/`。
- Pages 生产构建与部署成功；线上主页、文章、About、搜索、RSS、站点地图、favicon 和 Pagefind 资源均返回 200。
- 在线 Pagefind 使用 `Agentic RL` 查询可返回正确文章，首页 canonical、中文标题和在线简历链接均正确。

## 2026-08-19：品牌命名与维护流程

- 将站点标题、作者、首页、About、无障碍文本与项目文档中的姓名统一为 `Li Zengsheng`，站点名称统一为 `Li Zengsheng's Blog`。
- 更新静态社交预览图，保留 LZS 标识并使用新的 Blog 名称。
- 新增内容更新与部署指南，明确文章、前端页面和链接的修改、验证、提交、推送与 Pages 确认流程。
- 新增线上链接巡检脚本，可检查站内页面、资源、锚点和外部 HTTP(S) 链接，并将反爬限制与明确失效分开报告。
- 将 404、RSS、站点地图和 robots 纳入固定巡检入口，并修正 404 页 canonical 指向不存在目录的问题。

## 2026-08-19：内容清理与可维护性审查

- 按确认结果删除 About 页四个失效项目链接及 Agentic RL 文章中的 OpenReview PDF 链接，并同步清理过期的“公开项目”文案。
- 将首页 RSS 入口并入“联系我”图标组，避免长标题和移动端出现孤立图标。
- 修复主题按钮被脚本覆盖成英文 `light/dark` 的无障碍标签，并将 RSS 辅助文本改为中文。
- 新增 `pnpm run verify`，统一执行格式检查、lint 和完整生产构建；README 与更新指南已同步。
- 链接巡检脚本兼容 pnpm 的自定义地址参数，可直接检查本地预览站点。
- 按页面精简要求移除首页可见的 RSS 图标与链接；RSS Feed 和浏览器自动发现元数据继续保留。

## 2026-08-20：首页首屏视觉协调

- 精简首页定位与导览文案，统一中文行高、段落间距和阅读宽度。
- 按最终表述移除“前沿学习”，并将“关于页面”入口文字精简为“关于”。
- 将在线简历、GitHub 和邮箱合并为同一行动区，建立主按钮与次级图标按钮的视觉层级。
- 为社交链接增加首页专用样式与外链安全属性；页脚继续使用原有默认样式，内容与布局保持不变。

## 2026-09-15：阅读体验与简历更新

已实现侧边目录与移动抽屉、复制按钮样式计算优化、文章生命周期清理、图片及 Mermaid 延迟加载；新增第七篇训练机制文章，固定 slime 参考版本并纠正分段权重与双沙箱论述。首次构建通过，七篇进入搜索索引；浏览器初查无页面错误或水平溢出。最终 `pnpm run verify` 通过。七篇文章共 125 处公式、11 张 Mermaid 图的明暗渲染检查通过；图表加载失败保留源码。验证列表进入、章节定位、历史恢复、代码复制、手机抽屉及 Esc 焦点返回、无 JS 目录，未发现页面错误。

- Hot100 同机阻断外部图片的三次就绪时间为 850 / 836 / 843 ms，中位数 843 ms；4 倍 CPU 降速为 4305 ms。原基线为 24,924 ms，其中样式计算约 24.4 秒。数据反映本地浏览器条件，不代表所有网络环境。
- 长距离目录跳转改为即时定位；移动目录与回顶按钮分开，回顶监听并入统一生命周期。
- 本地 51 个站内页面与资源无失效；四个尚未发布的线上 canonical 地址将在推送后核对。外部参考站点存在 403/405 限制，不按站内故障处理。
- 按仓库约定提交、推送到 origin/main，并以 Pages 成功及线上页面核对作为发布验收。

## 2026-09-17：作者文案更新

- 发布作者修改的站点描述、首页标题与介绍、关于页正文，以及两篇 CodeAgent-RL 文章开头的精简。
- 修复关于页 frontmatter 描述缺失的闭合引号，保留作者文案。
- `pnpm run verify` 通过，生产构建生成 40 个页面，搜索索引仍收录七篇文章；本批仅涉及文案及 Markdown 语法修复。
